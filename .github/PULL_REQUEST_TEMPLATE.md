## What changed

<!-- e.g. "Updated WAT.ai's Fall 2026 deadline" or "Added Waterloo XYZ Club" -->

## Checklist

- [ ] I updated `lastVerified` to today's date on any cycle I changed.
- [ ] `status` accurately reflects reality (`Open`, `Upcoming`, `Closed`, or `Rolling`).
- [ ] Any URLs I added (`website`, `instagram`, `discord`, `applyUrl`) work and go to the right place.
- [ ] If I added a new club, the filename matches the `slug` field (e.g. `data/orgs/my-club.json` has `"slug": "my-club"`).
